<%= form_for(@blog_user) do |f| %>
  <% if @blog_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@blog_user.errors.count, "error") %> prohibited this blog_user from being saved:</h2>

      <ul>
      <% @blog_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
